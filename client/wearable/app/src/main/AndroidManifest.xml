<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.measuredatacompose">
    <!-- Required for HR sensor -->
    <!-- 이 권한은 앱이 사용자의 신체 센서 데이터에 접근할 수 있도록 허용합니다. 예를 들어, 심박수, 스트레스 수준 등과 같은 생체 측정 데이터를 읽거나 모니터링하는 데 사용될 수 있습니다. -->
    <uses-permission android:name="android.permission.BODY_SENSORS" /> <!-- add : orthh -->
    <!-- 이 권한은 앱이 사용자의 활동을 인식하고 추적할 수 있도록 허용합니다. 가속도계 및 다른 센서를 사용하여 걷기, 달리기, 자전거 타기 등과 같은 활동을 감지하고 분류하는 데 사용될 수 있습니다. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" /> <!-- add : internet permission(orthh) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.type.watch"
        android:required="true" />

    <queries>
        <package android:name="com.google.android.wearable.healthservices" />
    </queries>

    <application
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".MeasureActivity"
            android:exported="false"
            android:theme="@android:style/Theme.DeviceDefault.NoActionBar" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>